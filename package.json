{
  "name": "wifi-qr-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:web": "pnpm --filter wifi-qr-web dev",
    "build:web": "pnpm --filter wifi-qr-web build",
    "start:cli": "pnpm --filter @wifi-qr/cli start",
    "build:cli": "pnpm --filter @wifi-qr/cli build"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "tsx": "^4.21.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["canvas", "esbuild"]
  }
}
